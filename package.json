{
  "name": "ramaltani-workspace",
  "version": "1.0.0",
  "private": true,
  "description": "RamalTani — Climate-Smart Farming Decision Platform Root Workspace",
  "scripts": {
    "dev": "npx -y concurrently -n \"backend,frontend\" -c \"blue,green\" \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\"",
    "dev:backend": "npm --prefix backend run dev",
    "dev:frontend": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "build:frontend": "npm --prefix frontend run build",
    "start": "npm --prefix backend run start",
    "lint": "npm --prefix frontend run lint"
  }
}
